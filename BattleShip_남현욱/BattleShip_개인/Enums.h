#pragma once


enum HitResult
{
	MISS = 1,
	HIT = 2,
	DESTROY = 3,

	DESTROY_AIRCRAFT = 3,
	DESTROY_BATTLESHIP = 4,
	DESTROY_CRUISER = 5,
	DESTROY_DESTROYER = 6 ,
};

enum BoardState
{
	NONE_STATE,
	SHIP_STATE,
	MISS_STATE,
	HIT_STATE,
	DESTROY_STATE,
	ERROR_STATE = -1,
};

enum ClientShipType
{
	AIRCRAFT,
	BATTLESHIP,
	CRUISER,
	DESTROYER,
};

enum AIState
{
	HUNT,
	TARGET,
};